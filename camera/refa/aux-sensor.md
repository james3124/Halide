# REF-A aux sensor (S5K3P9-class wide) - TEMPLATE complement to main-sensor.md
# Sensor: TODO-measured (rev + lens lot photo + CAMSS driver SHA + libcamera ver)
# Capability: TODO (resolution, CSI lane, ISP path, flash: none)
# Quality delta vs stock: TODO-measured (blind vote, same 5-scene rig as tuning sheet)
# Orientation: sensor_native_deg TODO (plumb-line target photo proves) |
#   mount_mirror TODO | android_sensor_orientation = (native - offset) mod 360 = TODO (show math)
# EXIF: Orientation always written; Make=HALIDE refa; Software=build ID; GPS only
#   with consent (absent, never zeroed); timestamp shutter-close (host delta <=2s).
# 4GB tier: aux features degraded-first on 4GB (main gate unchanged).
# Status: UNMEASURED.
