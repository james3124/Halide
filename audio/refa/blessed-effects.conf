# blessed audio effects (ch04 s30 single-EQ doctrine) - refa
# Exactly one EQ in playback chain (host). Android audio.effect = pass-through.
pass-through
flat
voice-clarity
bass-lite
# Any other combination: Settings shows "Unsupported audio tuning (resets to Flat)".
