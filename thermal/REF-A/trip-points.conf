# Thermal trip points - REF-A (thermal-management S1; ch.10 S4/S28 gates).
# Trip points committed in DT (thermal.dtsi include) with hysteresis + source
# comments (datasheet max-junction minus margin, margin stated). Missing-vs-stock
# zones explained per zone (driver missing + BUG link, or virtual-stock-zone).
skin_warn=43C
skin_throttle=45C
skin_shutdown=48C
soc_warn=75C
soc_throttle=85C
soc_critical=95C
battery_warn=38C
battery_throttle=40C
battery_shutdown=45C
hysteresis=2C
policy_ref=thermal HAL throttles CPU/GPU/charging in order; trips logged at THERMAL_* journal tag
margin_note=datasheet max-junction minus TODO-measured C margin (fill per SKU)
