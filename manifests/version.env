# version.env -- committed version-string inputs (ch.01 S294 copy format, ch.09 S8 match rule).
# halide-version.py needs git; builders/phone without git read THIS file instead
# (reproducible version strings must not depend on checkout state).
# scripts/version-check.sh emits the two-line About-copy block from these values;
# tests/test_version.py validates the emitted block. ASCII only.
HALIDE_VER=1.4.0
BUILD=20260910.2
SKU=refa
SLOT=A
KERNEL=6.6.52-halide
SPL=2026-09-09
MODEM=refa.03
CARRIER=UNTESTED
MANIFEST_SHA=00000000
SBOM_SHA=00000000
