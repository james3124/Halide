# ALS/prox thresholds (ch03 s-kernel DT + appendix-04A s-sensors) - refa TEMPLATE
# DT keys (keep matrix + DT in sync with layout.md):
proximity-near-level=TODO-measured (finger-cover 20x, 0 miss gate)
proximity-far-level=TODO-measured (hysteresis; hunting oscillation fails review)
als-lux-level1=TODO-measured (vs meter, +-15% gate)
als-lux-level2=TODO-measured (3 lux levels total vs meter)
als-lux-level3=TODO-measured
als-poll-hz=on-change (no fixed poll; driver on-change only)
prox-power-note=wake 0.05mA poll (matches inventory.md; wake needs power sign s32)
# Verify: 10s iio capture per sensor with stimulus (tilt/spin/magnet/light-cover);
# step-counter 100-step walk +-5; prox finger 20x; ALS 3 levels vs meter +-15%.
# Status: UNMEASURED.
