# DRM format modifiers allowlist (text-only, ref ch. graphics)
# Allowed: LINEAR only by default; AFBC/generic vendor mods need waiver.
# Allowlists per-panel in panel.json; unlisted modifier = reject.
# Ref: graphics/refa/panel.json; composer validates before atomic commit.
# No binary blobs here; update note + re-test on panel change.
