# HWC limits (text-only, ref ch. graphics)
max_layers=4
max_fps=60
power_note=exceeding layers/fps raises GPU+display rail; see power/refa/budget.md
fallback=composer CLIENT fallback logged; no silent jank acceptance
