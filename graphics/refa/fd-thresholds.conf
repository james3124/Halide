# fd thresholds (ch06 s29) - numbers that page
WARN_total_slope_per_2h=4
FAIL_dmabuf_syncfile_slope_per_1h=0
FAIL_taskcycle50_total_delta=10
FAIL_display_restart3_gem_delta=5
# WARN if total slope >4 FDs/hour over any 2h window.
# FAIL if dmabuf+syncfile slope >0 over 1h (zero-tolerance), or total delta
# >+10 after task-cycle x50 (halide-task-cycle --app org.fdroid --n 50 --interval 4s;
# 1s intervals forbidden - fabricate stall-leaks), or GEM +>5 after display-restart x3.
# Computed by halide-fd-report (least-squares, verdict PASS/WARN/FAIL(class:slope/h)).
