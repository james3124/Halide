# composer present-thread scheduling (ch06 s12/s25 + ch03 s31)
# Owner: Graphics. Applied by halide-composer systemd unit (ExecStartPre chrt).
# Exit contract for checker snippets in this file: 0 ok / 1 mismatch / 2 no proc.
SCHED_POLICY=SCHED_FIFO
PRESENT_PRIO=10
PRESENT_UCLAMP_MIN=30
PRESENT_WAKE=vblank-minus-margin
# margin source: graphics/refa/panel.json present_margin_ms (TODO-measured)
# Vsync source must be DRM vblank event, never timer-guess.
# Audit: chrt -p <present-tid> == FIFO 10; cat /proc/<tid>/uclamp_min == 30% scale.
# Below audio-irq (FIFO 20), above drawer-daemon (FIFO 5). See graphics/prio-map.txt.
