# Translator glossary (localization-workflow S3 handoff pack).
# Product verbs never transliterated inconsistently; disputes resolve toward the
# shorter string (mobile truncation is the enemy). Char limits: lockscreen <=24,
# banners <=90, drawer labels <=30.

HALIDE: product name, never translated
Phosh: shell name, never translated
drawer: app grid (translate with screenshot ref tests/goldens/composer)
slot: A/B update slot (keep technical, do not paraphrase as "place")
wipe: crypto-erase (must read destructive in every locale)
sideload: ADB update install (keep technical term + local gloss)
dogfood: internal test fleet (translate as "test program" + note)
quarantine: flaky-test holding list (not medical isolation)
verified-boot: boot integrity check (keep paired with VB-R1/VB-R2 codes)
Contacts-fixture: Ada Example, +1555 numbers only (no real user data ever)
