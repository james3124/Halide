# drawer/hide.conf — hidden drawer entries (ch.05 §13)
# Format: package # reason. Unhide for testers: halide-drawer --show-all.
com.android.dialer # stock Dialer — GNOME Calls owns dialer (ch.05 §8)
com.android.messaging # stock SMS — Chats is store of record
com.android.browser # stock Browser — Epiphany default
com.android.vending # Play Store — never shipped (ch.01 §12)
com.google.android.gms # GMS — never shipped (ch.01 §12)
com.google.android.gsf # GMS framework — never shipped
