# rf/refa/pta.conf -- PTA priority params for SKU refa (wifi-bt-coexist S2)
# Firmware params encoding: SCO voice > WLAN latency-sensitive > A2DP > WLAN bulk.
# FW version pinned; fw bump re-validates the S3 matrix fully (no "same params" assumption).
# ASCII only. Values below are PLACEHOLDERS -- TODO from datasheet + lab proof.

[pta]
fw_version = TODO
sco_voice_priority = TODO-highest
wlan_latency_priority = TODO
a2dp_priority = TODO
wlan_bulk_priority = TODO-lowest
nm_scan_deferral_during_sco_s = TODO-max-30
retransmit_cap_vowifi_call = TODO (documented tradeoff: A2DP glitch vs call drop, call wins)
status = UNTESTED
