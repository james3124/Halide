# carrierB_roam.cfg -- callbox profile: restricted bands + roaming flag (rf-lab-spec S3)
# ASCII only.

tech = LTE restricted bands + roaming flag
bands = TODO (restricted set per test plan)
apn = <visited-network APN only if blessed for that pair; else data stays off (S16/S21 rule)>
host_profile_hash_required = true
carrier_blessed_fw_required = true
pass_gate = correct profile select, no silent fallback
proves = roam/wildcard behavior
