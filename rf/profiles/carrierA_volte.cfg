# carrierA_volte.cfg -- callbox profile: LTE + IMS VoLTE path (rf-lab-spec S3)
# ASCII only.

tech = LTE + IMS
bands = TODO (per carrierA band allowlist)
apn = <must-match host carrier profile>
ims_apn = <must-match host profile ims_apn>
volte = on
host_profile_hash_required = true
carrier_blessed_fw_required = true (see carrier/<id>/modem-fw.json; mismatched FW + profile voids run)
pass_gate = MT/MO call + SMS + IMS registered >=5min
proves = voice over IMS path
