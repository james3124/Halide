# gnss-sanity.cfg -- callbox profile: GNSS sim GPS L1 (rf-lab-spec S3)
# GNSS sanity runs with callbox at low power or off (LTE harmonics vs GPS L1); record level.
# ASCII only.

tech = GNSS sim (GPS L1)
scenario_file = TODO (static scenario per take)
callbox_level = low-power-or-off (record level)
pass_gate = TTFF + C/N0 floor per gnss-deepdive
proves = antenna path alive
