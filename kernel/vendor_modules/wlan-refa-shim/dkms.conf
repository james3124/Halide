PACKAGE_NAME="wlan-refa-shim"
PACKAGE_VERSION="0.1"
BUILT_MODULE_NAME[0]="wlan_refa_shim"
DEST_MODULE_LOCATION[0]="/extra"
AUTOINSTALL="no"
# DKMS equivalent for builder reproducibility (§4); module signed post-build per §24
# (signing order logged, depmod regenerated POST-sign — never reuse pre-sign artifacts).
