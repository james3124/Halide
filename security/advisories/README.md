# advisories/README.md -- published-advisory index (ch.08 S8 compromise runbook, S13, S29)
# Owner: Security * Advisory text ships the SAME HOUR the fix is published
# (wall + advisory atomic). Template: ../ADVISORY-TEMPLATE.md.
# No advisories filed yet -- rows below are the schema, not results.

| ID | CVE | Affected | Severity | Fix build | Embargo until | Credit |
|---|---|---|---|---|---|---|
| TODO (HSA-<YYYY>-<NNN>) | TODO | TODO (versions/SKUs) | TODO | TODO | TODO | TODO |

Rules: translations top-12; published here + transparency repo; user-runnable
verification command in every advisory (e.g. avbtool info_image --image
vbmeta.img | grep -i key); embargo breaches = P0 process bug.
